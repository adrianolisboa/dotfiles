{{- /* Rendered from your prompt answers; the real values live in ~/.config/chezmoi/chezmoi.toml, never in the repo. */ -}}
[user]
    name = {{ .workName }}
    email = {{ .workEmail }}
    signingkey = {{ .workName }} <{{ .workEmail }}>
